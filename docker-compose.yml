version: '3'

services:
  proxy:
    image: nginx:latest
    container_name: skyst_proxy.skyst.com
    depends_on:
      - server
      - socket
    ports:
      - "${PORT:?err}:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro

  db:
    image: mysql:8.0
    container_name: skyst_db.skyst.com
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:?err}
      - MYSQL_DATABASE=${MYSQL_DATABASE:?err}
      - MYSQL_USER=${MYSQL_USER:?err}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD:?err}

  server:
    build: .
    container_name: skyst_server.skyst.com
    depends_on:
      - db
    environment:
    - MYSQL_DATABASE=${MYSQL_DATABASE:?err}
    - MYSQL_USER=${MYSQL_USER:?err}
    - MYSQL_PASSWORD=${MYSQL_PASSWORD:?err}
    - MYSQL_HOST=skyst_db.skyst.com
    command: ["python3", "app.py"]
  
  socket:
    build: .
    container_name: skyst_socket.skyst.com
    depends_on:
      - db
      - server
    environment:
    - MYSQL_DATABASE=${MYSQL_DATABASE:?err}
    - MYSQL_USER=${MYSQL_USER:?err}
    - MYSQL_PASSWORD=${MYSQL_PASSWORD:?err}
    - MYSQL_HOST=skyst_db.skyst.com
    command: ["python3", "app_message.py"]